<!--pages/detail/detail.wxml-->
<view class="card-detail">
  <!-- {{scene}} -->
  <view class="card-list" wx:if="{{detail}}" wx:for="{{detail.img}}" wx:key="unique">
    <image src="{{item}}" mode="widthFix" data-url="{{item}}" catchtap="previewImg" />
  </view>
</view>

